export enum AppStrings {
    PORT = 'server.port',
    DB = 'db.url',
    DB_SYNC = 'db.synchronize',
    DB_TP = 'db.useUnifiedTopology',
    JWT_KEY = 'jwt.secret',
    JWT_EXPIRY = 'jwt.expiresIn',
    JWT_STRATEGY = 'jwt.strategy'
}